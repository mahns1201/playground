package springcontainer.member;import org.junit.jupiter.api.BeforeEach;import org.junit.jupiter.api.Test;import springcontainer.AppConfig;import static org.junit.jupiter.api.Assertions.*;class MemberServiceTest {    private MemberService memberService;    @BeforeEach    public void beforeEach() {        AppConfig appConfig = new AppConfig();        memberService = appConfig.memberService();    }    @Test    void join() {        //given        Member member = new Member(1L, "TestUser", Grade.BASIC);        //when        memberService.join(member);        //then        assertEquals(member.getId(), 1L);        assertEquals(member.getName(), "TestUser");        assertEquals(member.getGrade(), Grade.BASIC);    }    @Test    void findMember() {        //given        Member member = new Member(1L, "TestUser", Grade.BASIC);        memberService.join(member);        //when        Member foundMember = memberService.findMember(1L);        //then        assertEquals(foundMember.getId(), 1L);        assertEquals(foundMember.getName(), "TestUser");        assertEquals(foundMember.getGrade(), Grade.BASIC);    }}